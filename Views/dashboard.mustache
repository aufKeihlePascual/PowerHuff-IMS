<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/Views/dash-style.css">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        $(document).ready(function() {
            $(".sidebar a").click(function(e) {
                e.preventDefault();
                var content = $(this).data('content');
                $.get("/dashboard/content", { content: content }, function(response) {
                    $(".main-content").html(response);
                });
            });
        });
    </script>
</head>
<body>
    <div class="container">
        <div class="sidebar">
            <div>
                <h2>Inventory System</h2>
                <ul>
                    <li><a href="#" data-content="dashboard">Dashboard</a></li>
                    <li><a href="#" data-content="products">Products</a></li>
                    <li><a href="#" data-content="categories">Categories</a></li>
                    <li><a href="#" data-content="reports">Reports</a></li>
                </ul>
            </div>
            <div class="logout-button">
                <form method="GET" action="/logout">
                    <button type="submit">Logout</button>
                </form>
            </div>
        </div>
        <div class="main-content">
            <h1>Welcome, {{first_name}} {{last_name}}!</h1>
            <p>
                This is your inventory dashboard. Use the menu on the left to navigate through the system. You can manage your products, categories, and view reports from here.
            </p>
        </div>
    </div>
</body>
</html>
