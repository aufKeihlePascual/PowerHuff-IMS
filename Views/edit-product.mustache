<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/Views/css/dash-style.css">
    <link rel="stylesheet" href="/Views/css/products.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="/Views/js/dashboard.js"></script>
    
</head>
<body>
    <div class="dashboard-container">
        {{> sidebar}}
        <main class="main-content">
            {{> top-nav}}

            <!--------------------- Products Content --------------------->
            <section class="content">
                <h2>Edit Product</h2>
                <form action="/edit-product/{{product.Product_ID}}" method="POST">
                    <div class="form-group">
                        <label for="product_name">Product Name:</label>
                        <input type="text" id="product_name" name="product_name" value="{{product.Product_Name}}" required>
                    </div>
                    <div class="form-group">
                        <label for="price">Price:</label>
                        <input type="number" id="price" name="price" step="0.01" value="{{product.Price}}" required>
                    </div>
                    <div class="form-group">
                        <label for="stock_quantity">Stock Quantity:</label>
                        <input type="number" id="stock_quantity" name="stock_quantity" value="{{product.Stock_Quantity}}" required>
                    </div>
                    <div class="form-group">
                        <label for="lowstock_threshold">Lowstock Threshold:</label>
                        <input type="number" id="lowstock_threshold" name="lowstock_threshold" value="{{product.Lowstock_Threshold}}" required>
                    </div>
                    <div class="form-group">
                        <label for="supplier_id">Supplier:</label>
                        <select id="supplier_id" name="supplier_id" required>
                            {{#suppliers}}
                                <option value="{{supplier_id}}" {{#selected}}selected{{/selected}}>{{supplier_name}}</option>
                            {{/suppliers}}
                        </select>
                    </div>
                    <div class="form-group">
                        <label for="category_id">Category:</label>
                        <select id="category_id" name="category_id" required>
                            {{#categories}}
                                <option value="{{category_id}}" {{#selected}}selected{{/selected}}>{{name}}</option>
                            {{/categories}}
                        </select>
                    </div>
                    <button type="submit" class="btn-edit">Update Product</button>
                </form>
            </section>
        </main>
    </div>
</body>
</html>